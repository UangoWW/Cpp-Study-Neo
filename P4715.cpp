#include <iostream>
#include <algorithm>
#include <cstring>
#include <cstdio>
using namespace std;
typedef long long ll;
int a[130];
int tree[261];
int main(){
    // int n;
    // cin>>n;
    // int m=(1<<n);
    // for(int i=1;i<=m;i++){
    //     cin>>a[i];
    // }
    // for(int i=m;i<m*2;i++){
    //     tree[i]=i-m+1;
    // }
    // for(int i=m-1;i>=1;i--){
    //     if(a[tree[i*2]]>a[tree[i*2+1]])tree[i]=tree[i*2];
    //     else tree[i]=tree[i*2+1];
    // }
    // if(a[tree[2]]<a[tree[3]])cout<<tree[2];
    // else cout<<tree[3];

    // int n;
    // cin>>n;
    // int m=(1<<n);
    // int a[130]={};
    // for(int i=1;i<=m;i++){
    //     cin>>a[i];
    // }
    // int mid=m/2;
    // int amax=0,bmax=0,a1,b1;
    // for(int i=1;i<=mid;i++){
    //     if(a[i]>amax){
    //         amax=a[i];
    //         a1=i;
    //     }
    // }
    // for(int i=mid+1;i<=m;i++){
    //     if(a[i]>bmax){
    //         bmax=a[i];
    //         b1=i;
    //     }
    // }
    // if(amax>bmax)cout<<b1;
    // else cout<<a1;
    return 0;
}