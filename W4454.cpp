#include <iostream>
#include <algorithm>
#include <cstring>
#include <cstdio>
using namespace std;
typedef long long ll;
int p[205];
int main(){
    int n;cin>>n;
    for(int i=0;i<n;i++)cin>>p[i];
    sort(a,a+n);
    for(int i=)
    return 0;
}